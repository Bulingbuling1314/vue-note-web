const getters = {
    userInfo: (state: any) => state.user.userInfo,
    userMenu: (state: any) => state.user.userMenu,
    currentMenu: (state: any) => state.user.currentMenu
}

export default getters